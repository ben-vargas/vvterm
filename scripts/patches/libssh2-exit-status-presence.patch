--- a/include/libssh2.h
+++ b/include/libssh2.h
@@ -1031,6 +1031,8 @@
 #define libssh2_channel_flush_stderr(channel) \
     libssh2_channel_flush_ex((channel), SSH_EXTENDED_DATA_STDERR)
 
+/* Returns 1 only after a valid exit-status request was received. */
+LIBSSH2_API int libssh2_channel_has_exit_status(LIBSSH2_CHANNEL *channel);
 LIBSSH2_API int libssh2_channel_get_exit_status(LIBSSH2_CHANNEL* channel);
 LIBSSH2_API int libssh2_channel_get_exit_signal(LIBSSH2_CHANNEL* channel,
                                                 char **exitsignal,
--- a/src/libssh2_priv.h
+++ b/src/libssh2_priv.h
@@ -452,6 +452,7 @@
 
     /* channel's program exit status */
     int exit_status;
+    int exit_status_received;
 
     /* channel's program exit signal (without the SIG prefix) */
     char *exit_signal;
--- a/src/packet.c
+++ b/src/packet.c
@@ -1142,6 +1142,7 @@
                         channelp->exit_status =
                             _libssh2_ntohu32(data + 10 +
                                              strlen("exit-status"));
+                        channelp->exit_status_received = 1;
                         _libssh2_debug((session, LIBSSH2_TRACE_CONN,
                                        "Exit status %d received for "
                                        "channel %u/%u",
--- a/src/channel.c
+++ b/src/channel.c
@@ -1794,6 +1794,12 @@
     return channel->exit_status;
 }
 
+LIBSSH2_API int
+libssh2_channel_has_exit_status(LIBSSH2_CHANNEL *channel)
+{
+    return channel ? channel->exit_status_received : 0;
+}
+
 /*
  * libssh2_channel_get_exit_signal
  *
