# Servers and Workspaces

- Own server/workspace models, authoritative state, management, and related UI here.
- Use current Domain types rather than copied model definitions in documentation.
- Keep server/workspace limits in the application policy and state owners; UI renders those decisions instead of creating a second policy.
- For sync changes, also read [Core Sync rules](../../Core/Sync/AGENTS.md); for credential changes, read [Core Security rules](../../Core/Security/AGENTS.md).
