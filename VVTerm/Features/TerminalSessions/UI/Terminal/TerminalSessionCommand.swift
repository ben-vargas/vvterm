/// Commands shared by the terminal toolbar and the iOS Zen panel.
enum TerminalSessionCommand: String {
    case sessions
    case find
    case keyboard
    case toggleZen
    case settings
    case disconnect
}
